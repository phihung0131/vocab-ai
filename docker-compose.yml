services:
  vocab-ai:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - VITE_GEMINI_API_KEY=${VITE_GEMINI_API_KEY:-your_gemini_api_key_here}
    restart: unless-stopped 